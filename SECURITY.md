# 安全说明

请勿公开提交包含个人网络信息、访问凭据、节点配置或其他敏感数据的 Issue。

本项目不接收有关绕过访问控制、扫描未授权目标或压力测试的功能请求。安全问题请通过 [RR-vps 官方交流频道](https://t.me/GMgP4NG7lncwZGE1) 与维护者联系，并先隐藏敏感信息。

电脑端 Cloudflare 功能只接受 API Token，不接受全局 API Key。请创建仅限目标区域的 `DNS Edit` Token；只有在未填写 Zone ID、需要按域名查询区域时才增加 `Zone Read`。Token 不应写入域名文件、订阅链接、Issue、截图或日志。

本地网页服务强制绑定回环地址，并用每次启动随机生成的请求令牌保护写操作。域名订阅拒绝本机、内网、链路本地与保留地址，只允许 HTTP/HTTPS 的 80/443 端口和不超过 1 MiB 的文本内容。
